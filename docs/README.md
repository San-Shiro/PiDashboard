# Project Docs

This folder contains planning, design, and historical architecture notes for PiDashboard.

`LiteDashboard/` is the active implementation workspace. Older documents may mention top-level `core/`, `core_legacy_archive/`, or `src-anything/`; those paths are historical unless the document explicitly says it has been updated for `LiteDashboard/`.

For current setup and development entry points, start with the repository root `README.md` and `LiteDashboard/docs/`.
