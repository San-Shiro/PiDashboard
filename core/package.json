{
  "name": "pi-dashboard-workspace",
  "private": true,
  "workspaces": [
    "admin",
    "server"
  ],
  "scripts": {
    "dev:server": "bun --hot server/index.ts",
    "dev:admin": "npm run dev --workspace=admin",
    "build:admin": "npm run build --workspace=admin",
    "install:all": "npm install && bun install --cwd server"
  }
}
