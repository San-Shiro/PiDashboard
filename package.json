{
  "name": "pi-dashboard-workspace",
  "private": true,
  "workspaces": [
    "admin",
    "core/server"
  ],
  "scripts": {
    "dev:server": "bun --hot core/server/index.ts",
    "dev:admin": "npm run dev --workspace=admin",
    "build:admin": "npm run build --workspace=admin",
    "install:all": "npm install && bun install --cwd core/server"
  }
}
